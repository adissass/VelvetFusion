# k6 Benchmark Notes

This folder will contain k6 scripts used by the baseline contract.

Planned files:
- `baseline.js` for the fixed Phase 0 request mix

Expected environment variables:
- `BASE_URL` (default: `http://localhost:8080`)

Script implementation is intentionally deferred to the next checkpoint.
